import api from '@/services/api';

const state = {};
const mutations = {};
const actions = {};
const getters = {};

const productModule = {
  namespaced: true,
  state,
  mutations,
  actions,
  getters,
  modules: {},
};
export default productModule;
